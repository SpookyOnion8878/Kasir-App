# Base URL of the mock REST backend (json-server).
# In dev we proxy "/api" -> this target, so the app only ever calls "/api".
VITE_API_TARGET=http://localhost:3004

# Port used when you run `npm run server` (json-server).
API_PORT=3004
